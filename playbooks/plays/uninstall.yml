---

- name: uninstall k3s
  hosts: '{{ k3s_server_group | default("k3s_server") }}'

  tasks:

    - name: uninstall k3s
      ansible.builtin.include_role:
        name: k3s
        tasks_from: uninstall.yml
