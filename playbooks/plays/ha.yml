---

- name: deploy master node
  hosts: '{{ k3s_server_group }}[0]'

  roles:
    - role: k3s

- name: deploy server nodes
  hosts: '{{ k3s_server_group }}[1:]'
  serial: 1
  order: inventory

  roles:
    - role: k3s
